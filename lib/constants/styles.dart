import 'package:flutter/material.dart';

const shadowedStyle = const TextStyle(
  shadows: [
    BoxShadow(
      color: Colors.black,
      blurRadius: 6,
      spreadRadius: 1,
    )
  ],
  fontWeight: FontWeight.w500,
);
